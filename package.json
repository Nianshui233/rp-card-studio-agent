{
  "name": "rp-card-studio-agent",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "SillyTavern RP project Agent, private skills, and Forge toolchain.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:forge": "node scripts/build-forge.mjs",
    "check:forge": "node scripts/build-forge.mjs --check",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check:forge && node --check scripts/ui-app-builder.mjs && node --check scripts/rp-card-runtime.mjs && node --check scripts/rp-card-forge.bundle.mjs && node scripts/rp-card-forge.bundle.mjs doctor --json && npm test"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "esbuild": "0.25.9",
    "yaml": "2.9.0"
  }
}
