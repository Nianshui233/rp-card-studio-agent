schema_version: 1.0.0
mode: non_mvu
marker: "<回声创角>"
source: src/user-character.yaml
bindings:
  - input: name
    target: "profile.name"
  - input: identity
    target: "profile.identity"
  - input: relation
    target: "profile.station_relation"
commit:
  route: user_message
  readback: "提交文本与预览中的主控设定块一致"
  failure_fallback: "保留完整可复制的 <回声创角> 块"
