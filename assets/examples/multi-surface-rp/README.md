# 玻璃潮汐：多功能消息界面样本

覆盖同一项目中多个独立消息表面：

```text
<玻璃状态/>   → 状态页
<玻璃通知>…</玻璃通知> → 通知卡片
<玻璃选择>…</玻璃选择> → 选项面板
```

每个标记都有自己的生产者、消费者和回退，不把所有功能揉成一条无法调试的巨大正则。

`src/runtime/opening/创角变量桥.yaml` 规定所有页面共享同一份主控设定来源；状态、通知和选择页不能各自保存一份互不相认的创角数据。
