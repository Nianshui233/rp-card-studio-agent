schema_version: 1.0.0
status: locked
enabled: true
engine: st_prompt_template
host:
  extension: ST-Prompt-Template
  version: null
  features_evidence: ["生成前 EJS 与 manual/ejs_only 条目路线待实机确认"]
execution:
  phases: [generate_before]
  process_generation: true
  process_render: false
  process_raw_message: false
  process_code_blocks: false
variable_scopes: [mvu_stat_data_read, cache]
templates:
  - id: current_context
    file: src/runtime/mvu/current-context.ejs
    display_name: "议事厅当前状态上下文"
    phase: generate_before
    input: worldbook
    output: prompt
    reads: ["stat_data.世界", "stat_data.议案", "stat_data.舆论"]
    writes: []
    side_effect: mvu_read
    cache: none
    decorators: ["@@generate_before"]
    invokes_entries: []
    failure_fallback: "显示中文空态并保留普通世界书内容"
    runtime_evidence: []
decorators: ["@@generate_before"]
injections: []
worldbook_calls: []
bridges:
  - { from: ejs, to: mvu, access: read, path: "stat_data", source: current_message }
side_effects:
  allow_variable_write: false
  allow_raw_message_write: false
  allow_prompt_injection: false
  allow_worldbook_activation: false
diagnostics:
  syntax_checked: true
  runtime: not_run
  failure_behavior: "保留原文并显示中文空态"
dependencies: []
implementation_notes: ["EJS 与 MVU 分开维护；EJS 只读当前状态，不拥有状态写入权"]
source_refs: []
