<%_ const root = typeof stat_data === 'object' && stat_data ? stat_data : {}; _%>
当前议会时间：<%= root.世界?.当前时间 ?? '未初始化' %>
当前议案：<%= root.议案?.当前 ?? '未初始化' %>
舆论稳定度：<%= root.舆论?.稳定度 ?? '未初始化' %>
<%_ const called = getwi ? getwi('议案历史索引') : ''; _%><%= called || '' %>
