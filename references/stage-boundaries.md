# 阶段边界

阶段顺序：项目定位 → 材料整理（可选）→ 世界观 → 角色 → 系统（可选）→ 场景（可选）→ MVU/EJS（可选）→ 叙事与开场 → 状态栏/UI（可选）→ 整合交付。

每次只询问当前阶段能直接写入的内容。用户主动提供越界信息时记录到跨阶段待办，不继续追问。

| 阶段 | 当前阶段可以询问 | 当前阶段不要询问 | 实际产物 |
| --- | --- | --- | --- |
| 项目定位 | RP 形态、核心乐趣、节奏、气质、整体标题、卡面入口职责 | 具体世界规则、人物细节、变量、正则、UI 视觉 | 定位 YAML 与卡名/入口方向 |
| 材料整理 | 材料用途、可信度、冲突、保留/改写/舍弃 | 新创作细节 | 材料索引与冲突清单 |
| 世界观 | 规则、时代、地域、社会、势力、历史、知识层、世界自运转 | 具体人物人格、变量实现、UI | 可读世界 YAML |
| 角色 | 身份、目标、生活、心理、关系、行为、语言、知识、NSFW（如启用） | 新世界底层规则、变量路径、UI | 连续人物/NPC YAML |
| 系统 | RP 规则、循环、资源、代价、判定、后果、节奏 | MVU API、HTML、人物背景 | 系统 YAML/文本 |
| 场景 | 空间、氛围、行动、限制、变化、秘密、场外演进 | 开场文风、变量路径、前端组件 | 场景 YAML/文本 |
| MVU/EJS | 实际变量、初值、Schema JS、更新规则、输出格式、EJS、脚本、依赖与失败表现 | 新剧情、角色动机、视觉主题 | 真实 YAML/JS/EJS 文件 |
| 叙事与开场 | 人称、节奏、信息揭露、NPC主动性、默认/备选开场、初始化绑定、首条介绍/路线/创角前端的等级、视觉、流程、按钮与回退 | 进入RP后的持续状态/物品/关系/任务界面 | 叙事 YAML、开场正文/短标记、`opening_ui` 与完整开场 HTML |
| 状态栏/UI | 持续状态UI的轻/轻中/中/重/超重、主题、信息架构、数据映射、交互、响应式、宿主联动 | 首条介绍/创角前端、新世界/人物/系统语义 | `status_ui` 与持续消息 HTML/CSS/JS |
| 整合交付 | 世界书切分与调度、正则字段、脚本装配、制品、验证 | 新增重大创作设定 | assembly、JSON 和报告 |

## UI 首批问题

真正进入状态栏/UI阶段后，第一批只确认持续消息界面：

- 轻型、轻中型、中型、重型还是超重型；
- 主要玩家旅程；
- 主题与视觉签名；
- 桌面、移动或同等优先；
- 哪些内容组成一个完整功能面。

等级衡量视觉、交互和 JavaScript 深度，不按页面数或代码行数配额。不要设置“基础状态条兼容路线”来替代轻型 UI；用户只要简单状态条时，它就是一个范围较小但仍应完整、好看、可用的轻型实现。

## MVU/EJS 边界

系统阶段确定“状态代表什么、怎样变化”；MVU/EJS 阶段把已确定语义实现成真实文件。不要在 MVU/EJS 阶段重新设计世界或玩法，也不要把实际代码改写为通用配置。

## 叙事、开场与 UI 边界

叙事与开场阶段同时决定开场发生什么、模型看到什么、初始化是什么，以及首条介绍/创角前端如何呈现和交互。若创角表单会改变运行时数据，还要在该阶段定义 `creation_bridge` 的字段绑定、提交路线和读回。开场前端的视觉与按钮实现也在该阶段完成。

复杂开局通常使用短标记：display 正则替换成完整 HTML，prompt 正则给模型留下短说明。普通叙事开场直接投影到 `first_mes`/`alternate_greetings`。后续状态栏/UI阶段不得重新设计或接管该首楼应用，只处理进入RP后的持续消息界面。

## 跨阶段待办

```yaml
cross_stage_backlog:
  - id: backlog_example
    source_stage: worldbuilding
    target_stage: character
    summary: "列车长害怕明火；角色阶段决定成因和行为表现。"
    status: open
```

只记录用户已经说出的事实，不趁机扩写。进入目标阶段后消费并标记完成。

## 返回前置阶段

只有当前阶段确实无法可靠继续时，才返回前置阶段补最小缺口；修完后恢复当前阶段，不重做整个项目。

## NSFW

NSFW 只在预检确认一次。启用后自然并入相关阶段，不再设置问卷或运行门禁；不启用则不主动加载专项模板，但不净化已有材料。
